# Артефакти збірки — робляться з коду, у git їм не місце.
dist/

# Картинки й розмітка, які пише самоперевірка рендера (--selftest, --dump-html).
# А от її вхід, render/samples.json, лежить у git: скрипта, що генерував його
# за часів Python, більше немає.
render/*.png
render/*.html
