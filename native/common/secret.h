// Спільні «секрети» інжектора: маркер нашої DLL і токен запуску.
//
// Навіщо. Інжектор — потенційно небезпечний інструмент: технічно він уміє
// вкласти БІБЛІОТЕКУ в чужий процес. Щоб ним не скористалися сторонні для
// читів в онлайн-іграх, він:
//   1) працює лише з токеном, який передає наша програма (не запускається просто так);
//   2) вантажить ЛИШЕ нашу overlay-DLL з власної теки — файл мусить містити
//      маркер нижче; чужу (наприклад, чит-) DLL він не візьме;
//   3) НЕ вміє писати в память нічого, крім шляху до цієї DLL, і не виконує
//      нічого, крім LoadLibrary нашої ж бібліотеки, — тобто це не інструмент
//      «запиши будь-що в память», а строго «завантаж нашу overlay».
//
// Повний захист дало б підписування коду (окреме рішення); тут — практична
// планка, яка відсікає звичайне зловживання.
#pragma once

// Мусить збігатися з рядком, який повертає HominkaOverlayMarker() в overlay.dll,
// і з міткою в hominka/inject.py.
#define HOMINKA_OVERLAY_MARKER "HOMINKA-OVERLAY-D7A1F3E9-b2c4-4a6e-9f10-chat-in-game"

// Токен, без якого інжектор не працює. Дзеркало в hominka/inject.py.
#define HOMINKA_INJECT_TOKEN "HMK-INJ-7F3A9C21-64bd-4e0a-choose-your-game"
