// Предпросмотр для редактора CSS у Hominka на Python.
//
// Ані вікна, ані DirectComposition: чат малюється в память і йде пікселями
// назад каналом. Окремий процес, бо предпросмотр не має нічого спільного з
// оверлеєм на екрані — і не має права його чіпати.
//
// Потрібен лише старому шляху: у нативному редакторі теми правка лягає прямо
// в саму стрічку, і показувати схожу картинку більше ні до чого.
#pragma once

#include <windows.h>

namespace hominka {

int run_preview(DWORD parent_pid);

}  // namespace hominka
