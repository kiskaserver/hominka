// Зразки повідомлень для редактора теми.
//
// Навіщо: тему підбирають тоді, коли її видно, а чат мовчить саме тоді, коли
// над нею сидиш. Тож у стрічку можна підсипати кілька рядків — навмисно
// різних: із текстовими плашками й зі справжніми іконками, з емоутами, з
// грошима, відповідь, системна подія, звертання й довгий рядок. Стилі
// підбирають саме на такому наборі, а не на одному рядку.
//
// Картинки тут — «data:»-адреси, тобто самі себе й несуть: зразки мають
// показуватися й без мережі.
#pragma once

#include <vector>

#include "chat_doc.h"

namespace hominka {

std::vector<ChatMessage> demo_messages();

}  // namespace hominka
