"""
Hominka — прозорий оверлей чату поверх гри, НЕВИДИМИЙ для OBS.

Навіщо: у тебе один монітор, і чат, який OBS накладає на трансляцію, ти сам не
бачиш. Ця програма показує чат окремим вікном поверх усіх ігор, а Windows
приховує це вікно від будь-якого захоплення екрана (OBS Display/Window/Game
Capture його НЕ бачить — WDA_EXCLUDEFROMCAPTURE, Windows 10 2004+ / 11).

Це — точка входу, і нічого, крім запуску, тут немає: сам код живе в пакеті
`hominka/` (див. hominka/__init__.py — там карта того, що де лежить).

Запуск:  python chat_overlay.py  [URL]
"""

from hominka.app import main

if __name__ == "__main__":
    main()
