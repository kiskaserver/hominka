.venv/
config.json
__pycache__/
*.pyc
build/
dist/
build-linux/
dist-linux/
# Нативний рендер, зібраний контейнером linux/ по дорозі до AppImage.
dist-render/
# Специи PyInstaller генерирует сам при сборке по флагам — эти не нужны.
# Hominka_one.spec, наоборот, и ЕСТЬ описание сборки (один файл, заставка,
# отброшенные локали Chromium), и без него release.py собрать нечего.
Hominka.spec
profile/
.keys/
